let items = ["pen", "pencil", "ruler", "eraser", "book"];

console.log(items);

console.log(items[0]);
console.log(items[4]);

//adds element
items.push("textbook");

console.log(items);

//remove at end
items.pop();

console.log(items);

//remove at beginning
items.shift();

console.log(items);

let len = items.length;
console.log(len);

console.log(items);
